# Python Requirements for Disease Prediction Model
# Install with: pip install -r requirements.txt

# IMPORTANT: Python 3.13 has compatibility issues
# Recommended: Use Python 3.10 or 3.11
# If using Python 3.13, use these specific versions:

# Core ML libraries with version constraints for compatibility
torch>=2.0.0,<2.2.0
transformers>=4.35.0,<5.0.0
shap>=0.43.0,<0.45.0

# Additional dependencies with version constraints
numpy>=1.24.0,<1.27.0
scikit-learn>=1.3.0,<1.5.0
networkx>=3.0,<4.0

# Ensure compatible scipy version
scipy>=1.11.0,<1.13.0

# Optional: CUDA support (uncomment if using GPU)
# torch>=2.0.0+cu118 -f https://download.pytorch.org/whl/torch_stable.html

